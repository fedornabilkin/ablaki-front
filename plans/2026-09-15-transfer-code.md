# Хэш получения перевода

Цель: показать отправителю хэш получения в строке сформированного, ещё не полученного перевода.

Контекст: API уже возвращает поле `password` только отправителю. Используем существующий контракт без изменения создания и получения переводов.

## Группа A: Отображение

- [x] A1. Вывести непустой хэш рядом с суммой собственного неполученного перевода; в истории его скрыть.
- [x] A2. Сделать хэш выделяемым и переносимым на узких экранах.
- [x] A3. Обновлять список при смене сессии, игнорируя запоздалые ответы.

## Группа B: Проверка и публикация

- [x] B1. Выполнить unit-тесты и сборку: 119 тестов рабочей ветки прошли, Vite build успешен.
- [x] B2. Проверить контракт на тестовом API без создания новых переводов: у шести собственных неполученных переводов есть непустой код. Значения кодов в журнал не выводились.
- [ ] B3. Опубликовать рабочую ветку в test и проверить SHA выкладки.

Критерий готовности: хэш доступен отправителю в списке «Не получены», проверки проходят, изменение доступно на тестовом контуре.
